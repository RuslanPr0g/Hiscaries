name: .NET CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Install .NET Aspire workload
        run: dotnet workload install aspire

      - name: Restore dependencies
        working-directory: server/src
        run: dotnet restore Hiscary.sln

      - name: Build
        working-directory: server/src
        run: dotnet build Hiscary.sln

      - name: Test (non-containerized)
        working-directory: server/src
        run: dotnet test Hiscary.Security.Tests/Hiscary.Security.Tests.csproj --no-build --verbosity normal

      - name: Test (containerized integration)
        working-directory: server/src
        timeout-minutes: 15
        run: dotnet test Hiscary.UserAccounts.IntegrationTests/Hiscary.UserAccounts.IntegrationTests.csproj --no-build --verbosity normal
